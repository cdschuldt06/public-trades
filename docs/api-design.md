# PublicTrades API Design

## Base URL

Local development:

`http://localhost:3000`

## Endpoints

### GET /health

Purpose:
Checks that the backend server is running.

Response:

```json
{
  "status": "OK"
}